# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage
/test-results
/playwright-report
/.playwright

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (never commit DB credentials / secrets)
.env
.env*.local

# local DB dumps / backups (raw customer PII: phone numbers + transcripts — NEVER commit).
# NOTE: drizzle/*.sql migrations are DDL-only and ARE committed; these are data dumps.
/pepper-db-review.sql
/backups/
*.dump

# typescript
*.tsbuildinfo
